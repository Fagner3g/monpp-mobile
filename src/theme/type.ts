export interface IStyledTheme {
  colors: {
    primary: string
    secundary: string

    text: string
    placeholder: string

    danger: string
    warning: string

    border: string

    background: string
  }
}
